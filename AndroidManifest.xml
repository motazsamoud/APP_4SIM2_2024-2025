<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <application
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"
        tools:targetApi="31">


        <activity
            android:name=".view.stat.Statistique">
            <!-- Add any other necessary configurations here -->
        </activity>
        <activity
            android:name=".view.activiteList.ActiviteRecyclageListUser">
            <!-- Add any other necessary configurations here -->
        </activity>

        <activity
            android:name=".view.activiteList.ActiviteRecyclageList">
            <!-- Add any other necessary configurations here -->
        </activity>
        <activity
            android:name=".view.LoginActivite.ChangePasswordActivity">
            <!-- Add any other necessary configurations here -->
        </activity>



        <activity
            android:name=".view.updateActivite.UpdateActiviteRecyclage">
            <!-- Add any other necessary configurations here -->
        </activity>
        <activity
            android:name=".view.ratingActivite.AddRatingActivite">
            <!-- Add any other necessary configurations here -->
        </activity>
        <activity
            android:name=".view.addActivite.AddActiviteRecyclage">
            <!-- Add any other necessary configurations here -->
        </activity>
        <activity
            android:name=".view.chatbot.ChatbotActivity">
            <!-- Add any other necessary configurations here -->
        </activity>

        <activity
            android:name=".view.LoginActivite.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data
                android:name="android.app.lib_name"
                android:value="" />

            <!-- Ajoutez cette ligne pour l'utilisation d'AWS Mobile Client -->
            <meta-data
                android:name="com.amazonaws.mobile.client.app_id"
                android:value="34JIE0MYUD"/> <!-- Remplacez par votre ID d'application -->

        </activity>



        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
